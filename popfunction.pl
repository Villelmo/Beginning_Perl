# the pop function pops and returns the last value off the 
# end of an array. The array length is shortened by one 
# element
my @array = (2,4,6,8,10);
my $last_element = pop @array;
print $last_element;

